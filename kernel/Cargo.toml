[package]
name = "thingos"
version = "0.1.0"
edition = "2024"

[dependencies]
limine = "0.4"
linked_list_allocator = "0.10"
spin = "0.10.0"
x86_64 = { version = "0.15.2", default-features = false, features = [
    "instructions",
] }

[features]
default = []
