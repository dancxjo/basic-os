# Drivers

This directory will eventually contain all device driver crates (framebuffer, keyboard, mouse, etc.), leaving `apps/` for user-facing applications. Until refactors are complete, some driver crates may still live under `apps/`.
