[package]
name = "app-keyboard-driver"
version = "0.1.0"
edition = "2024"

[dependencies]
userland = { path = "../../userland" }
uuid = { version = "1.10", default-features = false, features = [
    "v5",
    "serde",
] }
