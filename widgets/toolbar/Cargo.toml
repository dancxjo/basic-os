[package]
name = "widget_toolbar"
version = "0.1.0"
edition = "2021"

[dependencies]
userland = { path = "../../userland" }
thing_abi = { path = "../../thing_abi" }

[features]
default = []
std = ["userland/std"]
